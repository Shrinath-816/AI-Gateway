# Web framework and ASGI server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Settings management (typed env var loading)
pydantic>=2.9.0
pydantic-settings>=2.5.0

# LLM client — OpenRouter exposes an OpenAI-compatible API, so we use
# the official OpenAI Python SDK pointed at OpenRouter's base_url.
openai>=1.50.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
